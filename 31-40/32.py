# 32)나누어 떨어지는 수 - 어려움
# 숫자 n과 m이 입력으로 주어집니다. N이 m으로 나누어 떨어지는지
# 알아내는 함수 isDivide(n,m)을 만들어 봅시다.
# 단, 이번에는 %, //, /, divmod()를 사용하지 않고 풀어 봅시다.

# 예)isDivide(6,3) => True 리턴
# isDivide(5,2) => False 리턴

#여기에 함수를 만듭니다.

#함수 실행이 되는지 확인하는 부분입니다.
print(isDivide(6, 3)) #=> True 출력
print(isDivide(5,2)) #=> False 출력